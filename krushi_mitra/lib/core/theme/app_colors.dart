import 'package:flutter/material.dart';

/// Kharif Gold Premium Design System — Colors
/// "The Digital Agronomist" color palette from Stitch
class AppColors {
  // ── Primary (Forest Green) ──────────────────────────────────
  static const Color primary = Color(0xFF91D78A);
  static const Color primaryContainer = Color(0xFF1B5E20);
  static const Color onPrimary = Color(0xFF003909);
  static const Color onPrimaryContainer = Color(0xFF90D689);

  // ── Secondary (Teal) ────────────────────────────────────────
  static const Color secondary = Color(0xFF84D5C5);
  static const Color secondaryContainer = Color(0xFF00685B);
  static const Color onSecondary = Color(0xFF003730);

  // ── Tertiary (Harvest Gold) ──────────────────────────────────
  static const Color tertiary = Color(0xFFEBC33E);
  static const Color tertiaryContainer = Color(0xFFCDA722);
  static const Color onTertiary = Color(0xFF3C2F00);

  // ── Surface / Background ─────────────────────────────────────
  static const Color background = Color(0xFF09160A);
  static const Color surface = Color(0xFF09160A);
  static const Color surfaceDim = Color(0xFF09160A);
  static const Color surfaceBright = Color(0xFF2E3D2E);
  static const Color surfaceContainerLowest = Color(0xFF041106);
  static const Color surfaceContainerLow = Color(0xFF111F11);
  static const Color surfaceContainer = Color(0xFF152315);
  static const Color surfaceContainerHigh = Color(0xFF1F2D1F);
  static const Color surfaceContainerHighest = Color(0xFF2A3829);

  // ── On Surface ───────────────────────────────────────────────
  static const Color onBackground = Color(0xFFD6E7D2);
  static const Color onSurface = Color(0xFFD6E7D2);
  static const Color onSurfaceVariant = Color(0xFFC0C9BB);

  // ── Outline ──────────────────────────────────────────────────
  static const Color outline = Color(0xFF8A9386);
  static const Color outlineVariant = Color(0xFF41493E);

  // ── Error ────────────────────────────────────────────────────
  static const Color error = Color(0xFFFFB4AB);
  static const Color errorContainer = Color(0xFF93000A);

  // ── Semantic Shortcuts ────────────────────────────────────────
  static const Color harvestGold = Color(0xFFEBC33E);
  static const Color forestGreen = Color(0xFF1B5E20);
  static const Color leafGreen = Color(0xFF91D78A);
  static const Color deepTeal = Color(0xFF00695C);
  static const Color warningAmber = Color(0xFFE67E22);
  static const Color alertRed = Color(0xFFE74C3C);
  static const Color skyBlue = Color(0xFF2980B9);

  // ── Glassmorphism ─────────────────────────────────────────────
  static Color glassCard = const Color(0xFF2A3829).withOpacity(0.6);
  static Color glassBorder = const Color(0xFF41493E).withOpacity(0.4);
  static Color glassOverlay = const Color(0xFF041106).withOpacity(0.08);
}
